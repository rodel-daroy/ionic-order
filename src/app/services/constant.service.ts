import { environment } from './../../environments/environment';
import { Injectable } from '@angular/core';

@Injectable({
    providedIn: 'root',
})

export class ConstService {
    base_url(base_url: any): any {
        throw new Error("Method not implemented.");
    }
    
}